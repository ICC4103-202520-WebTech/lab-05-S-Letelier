<h1>Reservations</h1>
<h2> Here are the Reservations: </h2>

<h4>
  <% @reservations.each do |reservation| %>
    <li>
      Reservation <%= link_to reservation.code, reservation_path(reservation) %>    
    </li>
  <% end %>

</h4>

<p> </p>
<%= link_to "Guest", guests_path %>
<%= link_to "Rooms", rooms_path %>
<%= link_to "Services", services_path %>
